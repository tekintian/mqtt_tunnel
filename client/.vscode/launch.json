{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
           "program": "${workspaceRoot}",
            "cwd": "${workspaceRoot}",
            "args": [
             "client",
             "-expose=127.0.0.1:8000",
             "-tunnel=localhost",
             "-name=admin",
             "-username=admin",
             "-password=888888"
             ]
        }
    ]
}